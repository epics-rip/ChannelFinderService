#!/bin/sh -e


case "$1" in
  configure)
    source /etc/channelfinder/glassfish_config_create 
    ;;
  abort-upgrade)
    :
    ;; 
  abort-remove)
    :
    ;; 
  abort-deconfigure)
    :
    ;; 
  *) echo "$0: didn't understand"
     exit 0
    ;;
esac
exit 0 
